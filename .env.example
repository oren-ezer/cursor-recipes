# Environment variables for this repo are loaded from backend/.env
# (see FastAPI settings and Alembic).
#
# Copy the template and create your real env file:
#   cp backend/.env.example backend/.env
# Then edit backend/.env with your secrets.
